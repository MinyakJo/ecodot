//import
import { atom } from "recoil"

//atom
export const isMobileState = atom({
    key: "isMobile",
    default: null
})

export const moSideIsOpenState = atom({
    key: "moSideIsOpen",
    default: false
})